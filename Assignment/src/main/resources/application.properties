spring.application.name=User_Service

server.port=8081

# MongoDB connection — database name is specified in the URI
spring.data.mongodb.uri=mongodb://localhost:27017/userdb

# Notes microservice base URL for inter-service REST calls
notes.service.base-url=http://localhost:8082
